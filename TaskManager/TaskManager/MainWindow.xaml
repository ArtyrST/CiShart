<Window x:Class="SPR_411_TaskManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SPR_411_TaskManager"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <!--<DockPanel>
        <Button Background="Bisque" FontSize="25" FontWeight="Bold"
                DockPanel.Dock="Top">Click 1</Button>
        <Button DockPanel.Dock="Left">Click 2</Button>
        <Button DockPanel.Dock="Right">Click 3</Button>
        <Button DockPanel.Dock="Bottom">Click 4</Button>
        <Button DockPanel.Dock="Top">Click 5</Button>
        <Button>Click 6</Button>

    </DockPanel>-->
    <DockPanel>
        <ToolBar DockPanel.Dock="Top">
            <Button Click="Button_Click">Refresh</Button>
            <Button Click="Button_Click_1" >Test</Button>
            <Button >Kill</Button>
            <Button >Show Info</Button>
            <TextBox Width="300" Name="processName"></TextBox>
            <Button >Start process</Button>
        </ToolBar>
        <DataGrid x:Name="grid" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Process Name" Binding="{Binding ProcessName}" />
                <DataGridTextColumn Header="PID" Binding="{Binding Id}" />
                <DataGridTextColumn Header="Total Processor Time" Binding="{Binding TotalProcessorTime}" />
                <DataGridTextColumn Header="Priority" Binding="{Binding PriorityClass}" />
                <DataGridTextColumn Header="User Name" Binding="{Binding UserName}" />
            </DataGrid.Columns>
        </DataGrid>

    </DockPanel>
    <!--<Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        
        
        <Button Background="Green">Click</Button>
        <Button Background="Red" Grid.Column="1" Grid.RowSpan="2">Click</Button>
        <Button Background="Blue" Grid.Column="3" Grid.Row="1">Click</Button>
        <Button Content="Click me" Grid.Column="2" Background="Yellow" 
                Margin="10" Padding="5"/>
    </Grid>-->
</Window>